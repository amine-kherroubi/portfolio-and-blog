---
/**
 * Footer Component - Desktop Only (Refactored)
 *
 * Now uses shared format utility for consistent date formatting.
 */

import { getCurrentYear } from "@/utils/format";

interface Props {
  variant?: "default" | "light";
}

const year = getCurrentYear();
---

<footer
  class="py-8 px-8 bg-white text-black border-t-2 border-black"
  role="contentinfo"
>
  <div
    class="max-w-container mx-auto flex justify-between items-center text-sm font-bold uppercase tracking-[0.15em]"
  >
    <p class="opacity-60">© {year}</p>

    <div class="flex items-center gap-8">
      <a
        href="/rss.xml"
        class="opacity-60 hover:opacity-100 transition-opacity duration-200 focus-visible:opacity-100"
        aria-label="RSS Feed"
      >
        RSS
      </a>
      <p class="opacity-60">Built with Astro</p>
    </div>
  </div>
</footer>
