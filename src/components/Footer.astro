---
interface Props {
  variant?: "light" | "dark";
}

const { variant = "dark" } = Astro.props;
const year = new Date().getFullYear();
---

<footer
  class:list={[
    "py-8 px-8 lg:px-16 border-t-2",
    variant === "dark"
      ? "bg-black text-white border-white/20"
      : "bg-white text-black border-black",
  ]}
>
  <div
    class="max-w-7xl mx-auto flex justify-between items-center text-xs uppercase tracking-wider"
  >
    <p>© {year}</p>
    <p>Built with Astro</p>
  </div>
</footer>
