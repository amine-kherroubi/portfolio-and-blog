---
interface Props {
  variant?: "light" | "dark";
}

const { variant = "dark" } = Astro.props;
const year = new Date().getFullYear();
---

<footer
  class:list={[
    "py-8 px-16 lg:px-24 border-t-2",
    variant === "dark"
      ? "bg-black text-white border-white"
      : "bg-white text-black border-black",
  ]}
>
  <div
    class="max-w-[1600px] mx-auto flex justify-between items-center text-[0.6875rem] uppercase tracking-[0.15em] font-bold"
  >
    <p class="opacity-60">© {year}</p>
    <p class="opacity-60">Built with Astro</p>
  </div>
</footer>
