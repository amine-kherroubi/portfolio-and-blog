---
import Tag from "./Tag.astro";
import { getTagsByIds, type TagId } from "../config/tags";

interface Props {
  title: string;
  excerpt: string;
  date: string;
  readTime: string;
  slug: string;
  tags?: TagId[];
  variant?: "featured" | "list";
}

const {
  title,
  excerpt,
  date,
  readTime,
  slug,
  tags = [],
  variant = "list",
} = Astro.props;

const tagObjects = tags.length > 0 ? getTagsByIds(tags) : [];
---

{
  variant === "featured" ? (
    <article
      class="group bg-white border-4 border-black hover:bg-gray-50 transition-all duration-200"
      itemscope
      itemtype="https://schema.org/BlogPosting"
    >
      <a
        href={`/blog/${slug}`}
        class="block p-8 md:p-12 lg:p-16 h-full focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-black focus-visible:ring-offset-4"
        aria-label={`Read article: ${title}`}
      >
        <div class="flex items-center gap-4 md:gap-6 text-xs md:text-sm font-bold uppercase tracking-[0.15em] mb-6 md:mb-8">
          <time datetime={date} itemprop="datePublished">
            {date}
          </time>
          <span class="w-2 h-2 bg-black" aria-hidden="true" />
          <span itemprop="timeRequired">{readTime}</span>
        </div>

        <h3
          class="text-3xl md:text-4xl lg:text-5xl font-bold leading-tight mb-4 md:mb-6 group-hover:underline decoration-2 underline-offset-8"
          itemprop="headline"
        >
          {title}
        </h3>

        <p
          class="text-lg md:text-xl text-black/70 leading-relaxed mb-6 md:mb-8"
          itemprop="description"
        >
          {excerpt}
        </p>

        {tagObjects.length > 0 && (
          <div class="flex flex-wrap gap-2 mb-6 md:mb-8" role="list">
            {tagObjects.map((tag) => (
              <Tag label={tag.label} variant="default" />
            ))}
          </div>
        )}

        <span class="inline-flex items-center gap-3 font-bold uppercase text-xs md:text-sm tracking-[0.15em]">
          Read Article
          <svg
            class="w-5 h-5 md:w-6 md:h-6 transition-transform duration-200 group-hover:translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            stroke-width="3"
            aria-hidden="true"
          >
            <path
              stroke-linecap="square"
              stroke-linejoin="miter"
              d="M17 8l4 4m0 0l-4 4m4-4H3"
            />
          </svg>
        </span>
      </a>
    </article>
  ) : (
    <article
      class="border-b-2 border-black last:border-b-0"
      itemscope
      itemtype="https://schema.org/BlogPosting"
    >
      <a
        href={`/blog/${slug}`}
        class="group block py-12 md:py-16 lg:py-20 transition-colors duration-200 hover:bg-black hover:text-white focus-visible:bg-black focus-visible:text-white focus-visible:outline-none"
        aria-label={`Read article: ${title}`}
      >
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-y-6 gap-x-6 md:gap-x-12 lg:gap-x-16 items-start px-8 md:px-16 lg:px-20 xl:px-32">
          <time
            class="lg:col-span-2 text-xs md:text-[0.8125rem] font-bold uppercase tracking-[0.15em] opacity-60 group-hover:opacity-100 transition-opacity"
            datetime={date}
            itemprop="datePublished"
          >
            {date}
          </time>

          <div class="lg:col-span-7">
            <h3
              class="text-3xl md:text-[2.25rem] lg:text-[2.75rem] font-bold leading-[1.1] tracking-[-0.02em] mb-4 md:mb-5"
              itemprop="headline"
            >
              {title}
            </h3>
            <p
              class="text-lg md:text-[1.125rem] lg:text-[1.25rem] leading-[1.6] opacity-70 group-hover:opacity-100 transition-opacity mb-4 md:mb-6"
              itemprop="description"
            >
              {excerpt}
            </p>
            {tagObjects.length > 0 && (
              <div class="flex flex-wrap gap-2" role="list">
                {tagObjects.map((tag) => (
                  <Tag label={tag.label} variant="default" />
                ))}
              </div>
            )}
          </div>

          <span
            class="lg:col-span-3 flex items-center justify-start lg:justify-end text-xs md:text-[0.8125rem] uppercase tracking-[0.15em] opacity-60 group-hover:opacity-100 transition-opacity"
            itemprop="timeRequired"
          >
            {readTime}
          </span>
        </div>
      </a>
    </article>
  )
}

<style>
  /* Smooth transitions */
  @media (prefers-reduced-motion: no-preference) {
    article a {
      transition:
        background-color 0.2s ease,
        color 0.2s ease;
    }

    article svg {
      transition: transform 0.2s ease;
    }
  }
</style>
